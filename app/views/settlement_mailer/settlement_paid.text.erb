Hi @<%= @tribe.username %>,

Paystack sent <%= number_to_currency(@settlement.amount_cents / 100.0, unit: "#{@settlement.currency} ") %> to your linked payout account<%= @settlement.destination.present? ? " (#{@settlement.destination})" : "" %>.

View your payout dashboard: <%= @dashboard_url %>

— TribeTip
